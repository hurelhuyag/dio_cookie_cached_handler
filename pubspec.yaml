name: dio_cookie_caching_handler
description: An Dio interceptor stores cookies and puts back to every requests made.
version: 0.0.3
homepage: https://github.com/hurelhuyag/dio_cookie_cached_handler

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  dio: ^5.1.1
  path_provider: ^2.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
